# 测试验证报告（2026-09-23）

## 范围与环境

本次验证对象是 `report-platform/` 当前可运行版本，包括 PostgreSQL 项目事实与规则、章节验证、内置 30 类语料及局部知识图谱。前端和后端分别监听本机 `127.0.0.1:5173`、`127.0.0.1:8000`；测试使用独立的 `report_platform_test` 数据库。浏览器操作中的章节变更只执行了预览和取消，没有确认提交。

## 结果

| 检查项 | 方法与观察 | 结果 |
|---|---|---|
| 后端回归 | `cd backend && ../.venv/bin/pytest -q tests/test_platform.py`；覆盖项目隔离、规则计算、预览提交、章节映射及语料图谱 | **8 passed** |
| 前端构建 | `cd frontend && npm run build`，TypeScript 与 Vite 构建完成 | **通过** |
| 语料完整性 | 在内置复制件目录执行 `shasum -a 256 -c SHA256SUMS`；116 个列入清单的文件摘要均匹配 | **通过** |
| 运行中 API | `/api/health` 返回 `ok`；语料为 7 组、30 类、114 个产物文件；30 个类别接口均返回非空记录；完整性报告为 117 个实体文件、116 个清单摘要、0 个错误 | **通过** |
| 事实与章节 | 页面显示项目事实、计算值、引用及待复核项。对设备单价预览 `0`，候选显示 `0元/条` 与 `0.00万元/条`；取消并刷新后，已保存值仍为 `1234.50万元/条` | **通过** |
| 关系与冲突 | 第 14 类关系列表标明 1,666 条原始关系；图谱定位 `X001` 后只显示 `X001`、`N050`、`N051` 和 2 条有向边。点选 `N050` 可查看 `800 kW`、来源文件与原文位置 | **通过** |
| 原文回链 | 从 `N050` 位置点击“定位统一正文”，第 06 类显示字符区间 `16564–16636`，高亮资料 06A 的 `800kW` 摘录 | **通过** |

本次未发现可复现的功能故障，未修改应用代码或已保存的项目事实。

## 验证边界

- 8 项测试是后端集成测试；页面操作为现场人工交互核对，尚未建立浏览器自动化回归套件。
- 章节验证目前只覆盖 `C0052` 和 `C0083` 两段候选正文。通过上述检查不能推断其他章节已经具备可靠的自动写作能力。
- 内置语料的哈希一致性证明复制件未变更，不等于业务事实已由独立人员复核。页面也明确标记语料“未独立人工核对”，向量“未生成”。
- 构建存在大于 500 kB 的 JS 分包提示；测试存在 2 条上游库弃用提示，均未导致失败。
