# 人工核对清单（开发前冻结）

## 实验操作

- [ ] 在新 QA 项目中确认事实台账初始为空。先选 `cy_tray_20260918@v2` 为只读参考，再逐项审核拟复用的结构、规则、论断和写法；历史值没有进入项目事实。
- [ ] C0052 先按 `300000` 与 `254016` 录入，再测 `0` 和缺值；记录原输入、Decimal 结果、事实修订、规则版本、候选文本及闸门。
- [ ] C0083 用“新拓设备”和 `1234.50 万元/条` 作为**合成测试输入**，检查元/条显示为 `12,345,000`，旧供应商和旧报价范围完全未继承。没有新的报价原件时只允许待核对表达。
- [ ] X001 同时显示两份历史来源的 800/650 kW、`open` 状态、阻断范围；新项目未录入自己的配电证据时不把这两个历史值当成项目值。
- [ ] L002 能从论断定位到支撑节点、EV-01、C0027 及原文位置；明确显示 EV-01 只有报告内嵌摘录、没有完整原件、未独立复核。正文不能把需求预测写成订单。
- [ ] 晋江事故报告负例不能接收澄岳可研的产能、焊接线报价、供应商或 `min` 规则；不适用原因可见。若没有事故调查领域包，仅检查通用上传和手工写作，不声称自动写作成功。

## Plate 与交付

- [ ] 选章节、看来源、生成候选、取消并刷新：数据库报告版本、正文和项目事实不变。
- [ ] 重新生成并确认候选，在 Plate 输入 `/`，实际使用斜杠菜单；插入事实引用、编辑表格、撤销重做、保存并刷新后结构和 ID 仍在。
- [ ] 修改已引用的项目事实时先看影响预览；取消不变更。确认后旧正文的引用及相关结论进入待核对，重新核对前不允许伪装成已交付状态。
- [ ] 在来源/论断/冲突视图反向定位到实际章节段落；从 Plate 事实/证据引用能定位到稳定项目记录或语料记录与原文位置。
- [ ] 有冲突、缺值、无新证据的确定性结论被正式导出闸门阻断；如允许预审稿，封面与审计记录标出保留事项。解包 DOCX、解析 PDF 核对数值、表格、引用和状态一致。

## 30 类贡献复核

- [ ] 每一类报告实际 `included/cited/blocked` 的 artifact_id、record_id、使用阶段、目标段落或闸门影响；没有事件的类别填写“未证明”，不填零成本或无必要的绝对结论。
- [ ] 最小包、条件包、全量可用包使用同一事实水位和审查标准；遮蔽导致构建器异常时标为“实现耦合”，不要误记为写作必要。
- [ ] `embeddings.parquet` 的零行和未生成状态原样呈现；不计入语义检索贡献。
- [ ] 结果按本测试范围报告；单个虚构语料且缺独立人工金标，不报告跨行业写作准确率。
