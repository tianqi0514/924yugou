# 仓库整理与交付核查（2026-09-24）

发布目标：[tianqi0514/924yugou](https://github.com/tianqi0514/924yugou)。本轮范围是现状审计、补齐需求/设计/清单、可复现安装与源码整理；未把后续业务能力提前实现或记为通过。

## 文档与代码整理

- 新增完整[产品需求文档](产品需求文档.md)，31条带状态需求，覆盖用户、业务流程、30类展示、抽取、事实、规则、写作、Plate、交付和部署边界。
- 新增[详细设计](详细设计文档.md)，说明实际数据表、接口、状态/版本、项目边界、规则事务、写作包、模型、Plate、来源、导出和迁移限制。
- 新增[开发清单](开发清单.md)：30项当前功能及15项后续任务，给出状态、优先级、依赖与完成标准；新增仓库 `AGENTS.md` 要求后续按清单推进。
- 新增[30类实际应用清单](30类资料写作应用清单.md)，保留每类数据库身份、实际消费和未证实的用途。结论是30类全部入库，8类有段落回链、4类仅包内或核对、18类尚无本轮记录级写作消费；回链不等于必要性证明。
- 重写README，统一全新安装、运行、演示、模型配置、测试、备份和文档入口。根目录早期设计与用户样本保持原样，新基线收敛在仓库docs中。
- 新增 `backend/scripts/bootstrap.py`：先建业务表再导入语料，可选预置公开报告。修正了旧说明在全新数据库上先预置项目、业务表却尚未创建的顺序问题。
- 新增Python依赖锁文件，与既有前端锁文件一起复现依赖；模型预设地址改为本机示例，已保存设置没有改动。
- 添加Git忽略和属性：排除运行数据、密钥、备份、日志、个人导出及参考截图；冻结语料禁止行尾自动转换，以免破坏SHA-256。

## 本轮实际执行

使用只包含待发布文件的副本和新建Python虚拟环境。数据库始终为 `report_platform_test`，清理前断言名称；应用库未用于安装测试。上传目录和模型配置目录指向隔离副本。

| 检查 | 真实结果 |
|---|---|
| Python锁文件安装 | 新虚拟环境安装44个锁定依赖成功，后端editable安装成功 |
| 前端安装 | 源码副本 `npm ci` 成功，安装249包 |
| 空数据库初始化 | bootstrap成功；30类、117资产、3,462记录、728对象、1,666原始边、566派生边 |
| 公开报告预置 | 4份原件分别205/65/8/15页，项目事实为空 |
| 重复初始化 | `imported=false`，4个演示项目均“已存在”，未重复创建 |
| 启动生命周期与API | TestClient运行真实后端生命周期，health 200；5项目、4原件、0项目事实，语料完整性有效 |
| 模型配置针对性回归 | 在新虚拟环境和发布副本运行 `test_model_settings.py`，7/7通过；有7条依赖弃用警告 |
| 前端构建 | TypeScript/Vite通过，2,471模块；主包1,391.50KB、gzip432.15KB，仍有体积提示 |
| 浏览器交互回归 | 发布副本9/9通过（41.6秒）；API替身测试，不把它算作真实PG全链 |
| 文档/发布核查 | 30类表完整；新文档使用相对路径；检查链接、暂存文件、冻结字节与敏感模式 |

本轮没有重新跑完整55项后端回归；最近一次55/55通过的代码范围和真实浏览器/导出结果见[UI验收](UI体验优化与验收_2026-09-24.md)。本轮后端行为变化仅是新增初始化入口和泛化模型连接模板，分别通过上述空库/幂等/API检查与7项针对性测试。前端除去空白字符外无业务改动。

## 发布内容与排除范围

发布源码、启动配置、锁文件、当前PRD/设计/清单、历史验证记录、冻结虚构语料、公开报告测试快照及合成QA夹具。公开报告来源和摘要见[样本说明](../test-fixtures/public-reports/README.md)，资料使用边界见[NOTICE](../NOTICE.md)。

不发布现有应用数据库、任何dump、用户上传存储、模型加密文件/主密钥、真实内网端点、环境凭据、日志或个人导出。克隆后只有初始化产生的历史项目和可选的原件演示项目；不会出现原机器上已经编辑过的QA报告。历史验收中的本机成稿路径是当时记录，不是仓库附带的交付文件。

## 仍需完成

- 已审核完整写作包尚未送入模型；当前服务端回链不能代表模型已消费全部资料。
- 等价替换实验、真实新项目证据与更多类型验证不足，不能宣称30类都必要或可通用成稿。
- 规则/语料独立版本、不可变交付快照、拒用/取消/导出审计事件尚未补齐。
- 当前无登录和项目授权；本次为公开代码仓库交付，不是公网应用部署。

后续按[开发清单](开发清单.md)推进，不以文档完成代替功能完成。
