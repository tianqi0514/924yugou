# 规则与章节配置验收

实施前固定 NEXT-20 的输入、结果和失败条件。所有自动写入测试只在 `report_platform_test` 运行；内置澄岳仅作历史回归。

| 案例 | 固定输入及操作 | 预期 | 失败条件 |
|---|---|---|---|
| C1 发布门槛 | 新项目建立字段 demand、capacity、sales，规则 `min(demand, capacity)`，章节选择三项；先编辑后试算再发布 | 未试算不能发布；需求 300000、能力 254016 时 sales=254016；发布内容不可再编辑 | 草稿直接用于运行、测试失败仍能发布、自由执行表达式 |
| C2 版本回放 | 以 v1 建方案和运行；新草稿把 sales 改为 `max(demand, capacity)` 并发布 v2 | v1 运行及正文仍为 254016；显式以 v2 创建方案后新运行为 300000；章节材料映射绑定各自版本 | 旧运行被改写、未显式切换的方案自动升级 |
| C3 边界 | 有效需求0、空值、循环依赖、除零、套/元单位不符 | 0 正常计算；空值不可评估；错误不能发布且无半提交 | 空值当0，错误规则进入已发布版本 |
| C4 隔离与负例 | 非可研项目发布自己的一章；另一项目尝试引用该配置 | 只显示当前项目配置，不能使用澄岳供应商、行业章节或跨项目配置 | 历史模板自动继承或跨项目读取 |
| C5 写作操作 | 从已发布配置创建空输入方案，录入、运行、选择章节并加入 Plate，保存刷新 | 只使用配置选择的指标与运行结果；段落、表格有运行引用；配置详情能看字段/规则/章节版本 | 页面只是配置展示，章节仍调用固定澄岳分支或保存后丢引用 |

## 实际执行

| 案例 | 实际结果 | 证据 |
|---|---|---|
| C1 | 通过。草稿保存后方可试算；样例 300000 / 254016 得 254016；试算凭证绑定配置修订，发布后编辑返回 409 | `backend/tests/test_analysis_config.py`；真实浏览器项目“QA · 配置版本与跨类型写作验收 2026-09-26”中 v1“销售测算配置” |
| C2 | 通过后端版本回放：v2 的 `max` 得 300000，旧运行及旧报告仍绑定 v1 的 254016；未将 v2 自动切给旧方案 | `test_config_version_run_and_chapter_are_immutable` |
| C3 | 通过。0 得 0；缺值试算无发布凭证；循环、除零、单位不符及任意代码表达式被拒，失败后草稿修订不变 | `test_config_zero_missing_cycle_division_unit_and_project_isolation` |
| C4 | 通过归属与内容负例：另一项目读取/引用返回 404；新项目候选无澄岳供应商，配置只给出本项目选择的数值 | 同上及真实浏览器候选 |
| C5 | 通过。真实浏览器完成配置建立、试算发布、空方案、输入、预览取消、保存运行、查看公式、绑定报告、候选取消/重生、确认入 Plate 与刷新；表格和段落保留销售 254016 及运行引用 | `frontend/integration/analysis-config.spec.ts`；本机浏览器 `report=9eb584fb-470a-4485-890c-8be6c981546c` |

测试执行：独立 `report_platform_test` 后端全套 **123/123 通过**，前端现有受控 API 交互 **13/13 通过**，本轮真实前后端配置流程 **1/1 通过**，TypeScript 与 Vite 生产构建通过。测试数据库在执行前断言库名；真实浏览器的专用 QA 项目使用日常库，但未修改用户既有项目。首次集成测试的最后一项报告接口读取出现时序失败，补充了报告 ID 与 HTTP 状态的显式断言后复跑通过；该首次失败不计作通过记录。

浏览器验收期间修复了自定义分组字段在方案输入面板消失、结果预览/比较只显示固定键，以及通用配置计算成功却显示“不可评估”的问题。合成销售算例证明配置和写作链路可运行，尚未证明跨行业论证质量。通用配置目前没有条件句、证据门槛、模型起草或配置级审批；历史三章仍有自己的受审路径。迁移前备份在 `tmp/backups/analysis_config_before_20260926.dump`，本次只新增表，未进行独立恢复演练。
